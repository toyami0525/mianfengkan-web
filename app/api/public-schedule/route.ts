import {NextRequest,NextResponse} from 'next/server';
import {adminSupabase} from '@/lib/supabase-admin';
function bounds(month:string){if(!/^\d{4}-\d{2}$/.test(month))return null;const[y,m]=month.split('-').map(Number);if(m<1||m>12)return null;const nextDate=new Date(Date.UTC(y,m,1));return{start:`${month}-01`,next:`${nextDate.getUTCFullYear()}-${String(nextDate.getUTCMonth()+1).padStart(2,'0')}-01`}}
export async function GET(req:NextRequest){try{const month=req.nextUrl.searchParams.get('month')||'';const b=bounds(month);if(!b)return NextResponse.json({error:'月份格式錯誤'},{status:400});const db=adminSupabase();const{data:staff,error:staffError}=await db.from('staff').select('id,name,slug').eq('active',true).order('name',{ascending:true});if(staffError)throw staffError;const ids=(staff||[]).map(x=>x.id);if(!ids.length)return NextResponse.json({staff:[],rows:[]});const{data:rows,error}=await db.from('staff_work_calendar').select('staff_id,work_date,status,updated_at').in('staff_id',ids).gte('work_date',b.start).lt('work_date',b.next).order('work_date',{ascending:true});if(error)throw error;return NextResponse.json({staff:staff||[],rows:rows||[]},{headers:{'Cache-Control':'no-store'}})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'讀取排班失敗'},{status:500})}}
